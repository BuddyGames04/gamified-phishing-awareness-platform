[flake8]
max-line-length = 88
exclude =
    migrations,
    __pycache__,
    env,
    disenv,
    settings.py
extend-ignore =
    E203,  # whitespace before ':'
    W503   # line break before binary operator
